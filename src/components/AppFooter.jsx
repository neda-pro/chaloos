import { Box } from "@mui/material";
import { grey } from "@mui/material/colors";

const AppFooter = () => {
  return (
    <Box component="footer" sx={{ height: 150, bgcolor: grey[900] }}></Box>
  );
};

export default AppFooter;
